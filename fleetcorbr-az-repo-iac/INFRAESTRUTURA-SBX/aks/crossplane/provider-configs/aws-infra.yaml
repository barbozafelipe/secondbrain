apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: aws-infra
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: aws-infra-creds
      key: creds